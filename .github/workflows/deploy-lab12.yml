name: Deploy Lab 12 to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'fit5032-lab12/NOMASH-LIBRARY/**'
      - '.github/workflows/deploy-lab12.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-lab12
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: fit5032-lab12/NOMASH-LIBRARY

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: fit5032-lab12/NOMASH-LIBRARY/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_OPENWEATHER_API_KEY: ${{ secrets.VITE_OPENWEATHER_API_KEY }}

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: fit5032-lab12/NOMASH-LIBRARY/dist
          clean: true
